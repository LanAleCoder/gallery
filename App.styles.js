import {StyleSheet} from 'react-native';

const stylesApp = StyleSheet.create({
  main: {backgroundColor: '#000000', flex:1},
  textApp: {
    color: '#ffffff',
    textAlign: 'center',
    fontSize: 60,
    fontFamily: 'KaushanScript-Regular',
  },
});

export default stylesApp;
